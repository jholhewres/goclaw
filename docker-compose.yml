services:
  devclaw:
    build: .
    container_name: devclaw
    restart: unless-stopped
    volumes:
      - devclaw-state:/home/devclaw
      - ./skills:/home/devclaw/skills:ro
    environment:
      - TZ=America/Sao_Paulo
    ports:
      - "8080:8080"
      - "8090:8090"
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

volumes:
  devclaw-state:
